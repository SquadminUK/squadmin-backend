const lambda = require('../../../../src/handlers/invitation/respond/get-invitation');

describe('Test getInvitationResponseHandler', () => {
    
    it('should get user response', async () => {
        expect(1).toEqual(1);
    });
});