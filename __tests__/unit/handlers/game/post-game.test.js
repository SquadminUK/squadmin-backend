const lambda = require('../../../../src/handlers/game/post-game');

describe('Test postGameHandler', () => {
    it('should create game details', async () => {
        expect(1).toEqual(1);
    });
});