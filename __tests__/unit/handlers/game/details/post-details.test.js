const lambda = require('../../../../../src/handlers/game/details/post-details');

describe('Test postGameDetailsHandler', () => {
    it('should update game details', async () => {
        expect(1).toEqual(1);
    });
});