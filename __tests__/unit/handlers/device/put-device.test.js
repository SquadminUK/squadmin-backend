const lambda = require('../../../../src/handlers/device/put-device');

describe('Test putDeviceHandler', () => {
    
    it('should update user device details', async () => {
        expect(1).toEqual(1);
    }); 
    
});