const lambda = require('../../../../src/handlers/device/post-device');

describe('Test postDeviceHandler', () => {
    it('should create device details', async () => {
        expect(1).toEqual(1);
    });
});