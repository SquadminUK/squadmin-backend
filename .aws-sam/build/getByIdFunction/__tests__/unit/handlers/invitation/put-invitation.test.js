const lambda = require('../../../../src/handlers/invitation/respond/get-invitation');

describe('Test putInvitationResponseHandler', () => {
    
    it('should update user response to invite', async () => {
        expect(1).toEqual(1);
    });
})